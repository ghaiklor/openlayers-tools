1) Український;
2) Русский;
3) English;

Український
======================================
OpenLayersTools.Console.writeInfo, OpenLayersTools.Console.writeDebug, OpenLayersTools.Console.writeError, OpenLayersTools.Console.writeWarning(message) — ці всі функції подібні. Вони виводять інформаційне повідомлення, повідомлення для «дебагу», повідомлення з помилкою чи попередженням в консоль розробника.

Параметри:
(string)message — повідомлення, котре потрібно вивести.

Вертає:
null
-------------------
OpenLayersTools.Console.writeGroup(name, messages) — функція виводить в консоль групу повідомлень.

Параметри:
(string)name — ім'я групи, в котру поміщаються повідомлення.
(array)messages — масив, значеннями яких є текстове повідомлення(string)

Вертає:
null
