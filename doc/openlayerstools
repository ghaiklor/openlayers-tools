1) Український;
2) Русский;
3) English;

Український
======================================
OpenLayersTools(mapOptions) — конструктор класу, який створює та ініціалізує необхідний об'єкт для коректної роботи модуля.

Параметри:
(object)mapOptions — об'єкт, в якому передаються параметри для початкової ініціалізації карти та класу.

Вертає:
об'єкт, в якому зберігаються класи по роботі з треками, транспортними засобами та інше. Далі весь виклик необхідних функцій базується на викликах необхідних методів в необхідному класі.
